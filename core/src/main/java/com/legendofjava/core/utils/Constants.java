package com.legendofjava.core.utils;

public class Constants {
    // Virtual resolution for retro look (similar to NES/SNES)
    public static final float V_WIDTH = 400;
    public static final float V_HEIGHT = 240;
    
    // Pixels Per Meter for Box2D (if used later)
    public static final float PPM = 16f;
}
